require 'spec_helper'

describe OpalHotReloader do
  it 'has a version number' do
    expect(OpalHotReloader::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
